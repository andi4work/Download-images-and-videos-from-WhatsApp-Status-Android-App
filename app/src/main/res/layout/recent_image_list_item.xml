<?xml version="1.0" encoding="utf-8"?>
<vid.img.download.SquareLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="2dp">


    <ImageView
        android:id="@+id/image_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />

    <TextView
        android:id="@+id/save_image_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@color/colorDownloadBtnBck"
        android:drawableRight="@drawable/ic_save_white_24dp"
        android:gravity="center"
        android:padding="5dp"
        android:text="download"
        android:fontFamily="sans-serif"
        android:textColor="@color/colorWhite"
         />

    <TextView
        android:id="@+id/delete_image_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@color/colorDeleteBtnBck"
        android:drawableRight="@drawable/ic_delete_white_24dp"
        android:gravity="center"
        android:padding="5dp"
        android:fontFamily="sans-serif"
        android:text="delete"
        android:textColor="@color/colorWhite"
        android:visibility="gone" />


    <ProgressBar
        android:id="@+id/progress_bar_for_save"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="5dp"
        android:layout_marginRight="5dp"
        android:src="@drawable/ic_save_white_24dp"
        android:tint="#0cac00"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/play_image_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@android:drawable/ic_media_play"
        android:visibility="gone" />


</vid.img.download.SquareLayout>